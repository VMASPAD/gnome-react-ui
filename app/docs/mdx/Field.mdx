---
title: Field
---

# Field

## Examples

### Example 1 — Basic field with label

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="code">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
import * as Field from './field/index.parts';

export default function BasicField() {
  return (
    <Field.Root className="flex flex-col gap-1.5 w-64">
      <Field.Label className="text-sm font-medium text-gray-700">Username</Field.Label>
      <Field.Control
        className="rounded-md border border-gray-200 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-blue-500"
        placeholder="john_doe"
      />
    </Field.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Field.Root className="flex flex-col gap-1.5 w-64">
          <Field.Label className="text-sm font-medium text-gray-700">Username</Field.Label>
          <Field.Control className="rounded-md border border-gray-200 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-blue-500" placeholder="john_doe" />
        </Field.Root>
    </Tabs.Panel>
</Tabs.Root>

### Example 2 — Field with description and error

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="code">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
export default function FieldWithError() {
  return (
    <Field.Root invalid className="flex flex-col gap-1.5 w-64">
      <Field.Label className="text-sm font-medium text-gray-700">Email</Field.Label>
      <Field.Description className="text-xs text-gray-400">
        You will receive a confirmation email.
      </Field.Description>
      <Field.Control
        type="email"
        defaultValue="not-an-email"
        className="rounded-md border border-red-300 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-red-500 data-[invalid]:border-red-400"
      />
      <Field.Error className="text-xs text-red-500">
        Please enter a valid email address.
      </Field.Error>
    </Field.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Field.Root invalid className="flex flex-col gap-1.5 w-64">
          <Field.Label className="text-sm font-medium text-gray-700">Email</Field.Label>
          <Field.Description className="text-xs text-gray-400">You will receive a confirmation email.</Field.Description>
          <Field.Control type="email" defaultValue="not-an-email" className="rounded-md border border-red-300 px-3 py-2 text-sm outline-none" />
          <Field.Error className="text-xs text-red-500">Please enter a valid email address.</Field.Error>
        </Field.Root>
    </Tabs.Panel>
</Tabs.Root>

### Example 3 — Form with multiple fields

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="code">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
export default function MultiFieldForm() {
  return (
    <form className="w-72 space-y-4">
      <Field.Root required className="flex flex-col gap-1">
        <Field.Label className="text-sm font-medium text-gray-700">
          Full name <span className="text-red-500">*</span>
        </Field.Label>
        <Field.Control
          required
          placeholder="Jane Doe"
          className="rounded-md border border-gray-200 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-blue-500"
        />
      </Field.Root>

      <Field.Root className="flex flex-col gap-1">
        <Field.Label className="text-sm font-medium text-gray-700">Bio</Field.Label>
        <Field.Description className="text-xs text-gray-400">Max 160 characters.</Field.Description>
        <Field.Control
          render={<textarea rows={3} />}
          placeholder="Tell us about yourself..."
          className="rounded-md border border-gray-200 px-3 py-2 text-sm outline-none resize-none focus:ring-2 focus:ring-blue-500"
        />
      </Field.Root>

      <button type="submit" className="w-full rounded-md bg-blue-600 py-2 text-sm text-white font-medium hover:bg-blue-700">
        Save
      </button>
    </form>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[200px] items-center justify-center p-6 outline-none" value="preview">
        <div className="w-72 space-y-4">
          <Field.Root required className="flex flex-col gap-1">
            <Field.Label className="text-sm font-medium text-gray-700">Full name <span className="text-red-500">*</span></Field.Label>
            <Field.Control required placeholder="Jane Doe" className="rounded-md border border-gray-200 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-blue-500" />
          </Field.Root>
          <Field.Root className="flex flex-col gap-1">
            <Field.Label className="text-sm font-medium text-gray-700">Bio</Field.Label>
            <Field.Description className="text-xs text-gray-400">Max 160 characters.</Field.Description>
            <textarea rows={3} placeholder="Tell us about yourself..." className="rounded-md border border-gray-200 px-3 py-2 text-sm outline-none resize-none focus:ring-2 focus:ring-blue-500" />
          </Field.Root>
          <button type="button" className="w-full rounded-md bg-blue-600 py-2 text-sm text-white font-medium hover:bg-blue-700">Save</button>
        </div>
    </Tabs.Panel>
</Tabs.Root>

## FieldControl

The form control to label and validate.
Renders an `&lt;input&gt;` element.

You can omit this part and use any Base UI input component instead. For example,
[Input](https://base-ui.com/react/components/input), [Checkbox](https://base-ui.com/react/components/checkbox),
or [Select](https://base-ui.com/react/components/select), among others, will work with Field out of the box.

Documentation: [Base UI Field](https://base-ui.com/react/components/field)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **defaultValue** | `string \| number \| readonly string[]` | — | — |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **onValueChange** | `(...) => void` | — | Callback fired when the `value` changes. Use when controlled. |


---

## FieldDescription

A paragraph with additional information about the field.
Renders a `&lt;p&gt;` element.

Documentation: [Base UI Field](https://base-ui.com/react/components/field)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## FieldError

An error message displayed if the field control fails validation.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Field](https://base-ui.com/react/components/field)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **match** | `boolean \| keyof ValidityState` | — | Determines whether to show the error message according to the field’s<br/>[ValidityState](https://developer.mozilla.org/en-US/docs/Web/API/ValidityState).<br/>Specifying `true` will always show the error message, and lets external libraries<br/>control the visibility. |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## FieldItem

Groups individual items in a checkbox group or radio group with a label and description.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Field](https://base-ui.com/react/components/field)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **disabled** | `boolean` | `false` | Whether the wrapped control should ignore user interaction.<br/>The `disabled` prop on `&lt;Field.Root&gt;` takes precedence over this. |


---

## FieldLabel

An accessible label that is automatically associated with the field control.
Renders a `&lt;label&gt;` element.

Documentation: [Base UI Field](https://base-ui.com/react/components/field)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **nativeLabel** | `boolean` | `true` | Whether the component renders a native `&lt;label&gt;` element when replacing it via the `render` prop.<br/>Set to `false` if the rendered element is not a label (e.g. `&lt;div&gt;`).<br/><br/>This is useful to avoid inheriting label behaviors on `&lt;button&gt;` controls (such as `&lt;Select.Trigger&gt;` and `&lt;Combobox.Trigger&gt;`), including avoiding `:hover` on the button when hovering the label, and preventing clicks on the label from firing on the button. |


---

## FieldRoot

Groups all parts of the field.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Field](https://base-ui.com/react/components/field)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction.<br/>Takes precedence over the `disabled` prop on the `&lt;Field.Control&gt;` component. |
| **name** | `string` | — | Identifies the field when a form is submitted.<br/>Takes precedence over the `name` prop on the `&lt;Field.Control&gt;` component. |
| **actionsRef** | `RefObject<FieldRootActions>` | — | A ref to imperative actions.<br/>- `validate`: Validates the field when called. |
| **touched** | `boolean` | — | Whether the field has been touched.<br/>Useful when the field state is controlled by an external library. |
| **dirty** | `boolean` | — | Whether the field's value has been changed from its initial value.<br/>Useful when the field state is controlled by an external library. |
| **validate** | `(...) => string \| string[] \| Promise<string \| string[]>` | — | A function for custom validation. Return a string or an array of strings with<br/>the error message(s) if the value is invalid, or `null` if the value is valid.<br/>Asynchronous functions are supported, but they do not prevent form submission<br/>when using `validationMode="onSubmit"`. |
| **validationMode** | `FormValidationMode` | `'onSubmit'` | Determines when the field should be validated.<br/>This takes precedence over the `validationMode` prop on `&lt;Form&gt;`.<br/><br/>- `onSubmit`: triggers validation when the form is submitted, and re-validates on change after submission.<br/>- `onBlur`: triggers validation when the control loses focus.<br/>- `onChange`: triggers validation on every change to the control value. |
| **validationDebounceTime** | `number` | `0` | How long to wait between `validate` callbacks if<br/>`validationMode="onChange"` is used. Specified in milliseconds. |
| **invalid** | `boolean` | — | Whether the field is invalid.<br/>Useful when the field state is controlled by an external library. |


---

## FieldValidity

Used to display a custom message based on the field’s validity.
Requires `children` to be a function that accepts field validity state as an argument.

Documentation: [Base UI Field](https://base-ui.com/react/components/field)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **children*** | `(...) => ReactNode` | — | A function that accepts the field validity state as an argument.<br/><br/> |


---

