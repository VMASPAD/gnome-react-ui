---
title: Tabs
---

# Tabs

## Examples

### Example 1 — Basic tabs

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="preview">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
import * as Tabs from './tabs/index.parts';

export default function BasicTabs() {
  return (
    <Tabs.Root defaultValue="tab1" className="w-80">
      <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab value="tab1" className="relative flex h-10 min-w-[80px] cursor-pointer items-center justify-center px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 data-[active]:font-semibold outline-none">
          Account
        </Tabs.Tab>
        <Tabs.Tab value="tab2" className="relative flex h-10 min-w-[80px] cursor-pointer items-center justify-center px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 data-[active]:font-semibold outline-none">
          Settings
        </Tabs.Tab>
        <Tabs.Tab value="tab3" className="relative flex h-10 min-w-[80px] cursor-pointer items-center justify-center px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 data-[active]:font-semibold outline-none">
          Billing
        </Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200" />
      </Tabs.List>
      <Tabs.Panel value="tab1" className="p-4 text-sm text-gray-600">
        Account settings and profile information.
      </Tabs.Panel>
      <Tabs.Panel value="tab2" className="p-4 text-sm text-gray-600">
        Application and notification preferences.
      </Tabs.Panel>
      <Tabs.Panel value="tab3" className="p-4 text-sm text-gray-600">
        Payment methods and invoices.
      </Tabs.Panel>
    </Tabs.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Tabs.Root defaultValue="tab1" className="w-80 rounded-lg border border-gray-200 overflow-hidden">
          <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
            <Tabs.Tab value="tab1" className="relative flex h-10 min-w-[80px] cursor-pointer items-center justify-center px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 data-[active]:font-semibold outline-none">Account</Tabs.Tab>
            <Tabs.Tab value="tab2" className="relative flex h-10 min-w-[80px] cursor-pointer items-center justify-center px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 data-[active]:font-semibold outline-none">Settings</Tabs.Tab>
            <Tabs.Tab value="tab3" className="relative flex h-10 min-w-[80px] cursor-pointer items-center justify-center px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 data-[active]:font-semibold outline-none">Billing</Tabs.Tab>
            <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200" />
          </Tabs.List>
          <Tabs.Panel value="tab1" className="p-4 text-sm text-gray-600">Account settings and profile information.</Tabs.Panel>
          <Tabs.Panel value="tab2" className="p-4 text-sm text-gray-600">Application and notification preferences.</Tabs.Panel>
          <Tabs.Panel value="tab3" className="p-4 text-sm text-gray-600">Payment methods and invoices.</Tabs.Panel>
        </Tabs.Root>
    </Tabs.Panel>
</Tabs.Root>

### Example 2 — Tabs with icons

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="preview">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
import { LayoutGrid, FileText, Settings } from 'lucide-react';

export default function IconTabs() {
  return (
    <Tabs.Root defaultValue="grid" className="w-80">
      <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        {[['grid', LayoutGrid, 'Grid'], ['docs', FileText, 'Docs'], ['settings', Settings, 'Settings']].map(([value, Icon, label]) => (
          <Tabs.Tab key={value} value={value} className="relative flex h-10 cursor-pointer items-center gap-1.5 px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 outline-none whitespace-nowrap">
            <Icon size={14} />{label}
          </Tabs.Tab>
        ))}
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200" />
      </Tabs.List>
      <Tabs.Panel value="grid" className="p-4 text-sm text-gray-600">Grid layout content</Tabs.Panel>
      <Tabs.Panel value="docs" className="p-4 text-sm text-gray-600">Documentation content</Tabs.Panel>
      <Tabs.Panel value="settings" className="p-4 text-sm text-gray-600">Settings content</Tabs.Panel>
    </Tabs.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Tabs.Root defaultValue="grid" className="w-80 rounded-lg border border-gray-200 overflow-hidden">
          <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
            <Tabs.Tab value="grid" className="relative flex h-10 cursor-pointer items-center gap-1.5 px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 outline-none whitespace-nowrap"><Icons.LayoutGrid size={14} />Grid</Tabs.Tab>
            <Tabs.Tab value="docs" className="relative flex h-10 cursor-pointer items-center gap-1.5 px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 outline-none whitespace-nowrap"><Icons.FileText size={14} />Docs</Tabs.Tab>
            <Tabs.Tab value="settings" className="relative flex h-10 cursor-pointer items-center gap-1.5 px-4 text-sm font-medium text-gray-500 hover:text-gray-800 data-[active]:text-gray-900 outline-none whitespace-nowrap"><Icons.Settings size={14} />Settings</Tabs.Tab>
            <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200" />
          </Tabs.List>
          <Tabs.Panel value="grid" className="p-4 text-sm text-gray-600">Grid layout content</Tabs.Panel>
          <Tabs.Panel value="docs" className="p-4 text-sm text-gray-600">Documentation content</Tabs.Panel>
          <Tabs.Panel value="settings" className="p-4 text-sm text-gray-600">Settings content</Tabs.Panel>
        </Tabs.Root>
    </Tabs.Panel>
</Tabs.Root>

### Example 3 — Pill-style tabs

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="preview">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
export default function PillTabs() {
  return (
    <Tabs.Root defaultValue="all">
      <Tabs.List className="flex gap-1 rounded-full bg-gray-100 p-1">
        {['all', 'active', 'paused', 'archived'].map((tab) => (
          <Tabs.Tab
            key={tab}
            value={tab}
            className="px-4 py-1.5 rounded-full text-sm font-medium text-gray-500 capitalize cursor-pointer transition-colors hover:text-gray-800 data-[active]:bg-white data-[active]:text-gray-900 data-[active]:shadow-sm outline-none"
          >
            {tab}
          </Tabs.Tab>
        ))}
      </Tabs.List>
      <div className="mt-4 text-sm text-gray-600 px-1">
        <Tabs.Panel value="all">All items listed here.</Tabs.Panel>
        <Tabs.Panel value="active">Active items only.</Tabs.Panel>
        <Tabs.Panel value="paused">Paused items.</Tabs.Panel>
        <Tabs.Panel value="archived">Archived items.</Tabs.Panel>
      </div>
    </Tabs.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <div>
          <Tabs.Root defaultValue="all">
            <Tabs.List className="flex gap-1 rounded-full bg-gray-100 p-1">
              <Tabs.Tab value="all" className="px-4 py-1.5 rounded-full text-sm font-medium text-gray-500 capitalize cursor-pointer transition-colors hover:text-gray-800 data-[active]:bg-white data-[active]:text-gray-900 data-[active]:shadow-sm outline-none">All</Tabs.Tab>
              <Tabs.Tab value="active" className="px-4 py-1.5 rounded-full text-sm font-medium text-gray-500 capitalize cursor-pointer transition-colors hover:text-gray-800 data-[active]:bg-white data-[active]:text-gray-900 data-[active]:shadow-sm outline-none">Active</Tabs.Tab>
              <Tabs.Tab value="paused" className="px-4 py-1.5 rounded-full text-sm font-medium text-gray-500 capitalize cursor-pointer transition-colors hover:text-gray-800 data-[active]:bg-white data-[active]:text-gray-900 data-[active]:shadow-sm outline-none">Paused</Tabs.Tab>
              <Tabs.Tab value="archived" className="px-4 py-1.5 rounded-full text-sm font-medium text-gray-500 capitalize cursor-pointer transition-colors hover:text-gray-800 data-[active]:bg-white data-[active]:text-gray-900 data-[active]:shadow-sm outline-none">Archived</Tabs.Tab>
            </Tabs.List>
            <div className="mt-4 text-sm text-gray-600 px-1">
              <Tabs.Panel value="all">All items listed here.</Tabs.Panel>
              <Tabs.Panel value="active">Active items only.</Tabs.Panel>
              <Tabs.Panel value="paused">Paused items.</Tabs.Panel>
              <Tabs.Panel value="archived">Archived items.</Tabs.Panel>
            </div>
          </Tabs.Root>
        </div>
    </Tabs.Panel>
</Tabs.Root>

## TabsIndicator

A visual indicator that can be styled to match the position of the currently active tab.
Renders a `&lt;span&gt;` element.

Documentation: [Base UI Tabs](https://base-ui.com/react/components/tabs)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **renderBeforeHydration** | `boolean` | `false` | Whether to render itself before React hydrates.<br/>This minimizes the time that the indicator isn’t visible after server-side rendering. |


---

## TabsList

Groups the individual tab buttons.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Tabs](https://base-ui.com/react/components/tabs)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **loopFocus** | `boolean` | `true` | Whether to loop keyboard focus back to the first item<br/>when the end of the list is reached while using the arrow keys. |
| **activateOnFocus** | `boolean` | `false` | Whether to automatically change the active tab on arrow key focus.<br/>Otherwise, tabs will be activated using &lt;kbd&gt;Enter&lt;/kbd&gt; or &lt;kbd&gt;Space&lt;/kbd&gt; key press. |


---

## TabsPanel

A panel displayed when the corresponding tab is active.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Tabs](https://base-ui.com/react/components/tabs)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **value*** | `any` | — | The value of the TabPanel. It will be shown when the Tab with the corresponding value is active. |
| **keepMounted** | `boolean` | `false` | Whether to keep the HTML element in the DOM while the panel is hidden. |


---

## TabsRoot

Groups the tabs and the corresponding panels.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Tabs](https://base-ui.com/react/components/tabs)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **defaultValue** | `any` | `0` | The default value. Use when the component is not controlled.<br/>When the value is `null`, no Tab will be active. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **value** | `any` | — | The value of the currently active `Tab`. Use when the component is controlled.<br/>When the value is `null`, no Tab will be active. |
| **orientation** | `Orientation` | `'horizontal'` | The component orientation (layout flow direction). |
| **onValueChange** | `(...) => void` | — | Callback invoked when new value is being set. |


---

## TabsTab

An individual interactive tab button that toggles the corresponding panel.
Renders a `&lt;button&gt;` element.

Documentation: [Base UI Tabs](https://base-ui.com/react/components/tabs)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **value*** | `any` | — | The value of the Tab. |
| **disabled** | `boolean` | — | Whether the Tab is disabled.<br/><br/>If a first Tab on a `&lt;Tabs.List&gt;` is disabled, it won't initially be selected.<br/>Instead, the next enabled Tab will be selected.<br/>However, it does not work like this during server-side rendering, as it is not known<br/>during pre-rendering which Tabs are disabled.<br/>To work around it, ensure that `defaultValue` or `value` on `&lt;Tabs.Root&gt;` is set to an enabled Tab's value. |
| **nativeButton** | `boolean` | `true` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `false` if the rendered element is not a button (e.g. `&lt;div&gt;`). |


---

