---
title: Preview Card
description: A popup that appears when a link is hovered, showing a preview for sighted users.
---

# Preview Card

A popup that appears on hovering over a link, showing a preview of the linked content. The trigger renders a native `<a>` — designed exclusively for hover, not click.

<Tabs.Root className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm" defaultValue="preview">
  <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
  </Tabs.List>
  <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
    <PreviewCard.PreviewCardBasic />
  </Tabs.Panel>
  <Tabs.Panel className="p-0 outline-none" value="code">
    ```tsx
    import { PreviewCard } from "gnome-ui/preview-card";

    export function PreviewCardBasic() {
      return (
        <p>
          The principles of good{" "}
          <PreviewCard.Root>
            <PreviewCard.Trigger
              href="https://en.wikipedia.org/wiki/Typography"
              target="_blank"
            >
              typography
            </PreviewCard.Trigger>
            <PreviewCard.Portal>
              <PreviewCard.Positioner sideOffset={8}>
                <PreviewCard.Popup>
                  <img src="..." alt="..." />
                  <div>
                    <p>Typography</p>
                    <Icons.ExternalLink />
                    <p>
                      The art and science of arranging type to make written
                      language legible, readable, and appealing.
                    </p>
                  </div>
                </PreviewCard.Popup>
              </PreviewCard.Positioner>
            </PreviewCard.Portal>
          </PreviewCard.Root>{" "}
          remain essential in the digital age.
        </p>
      );
    }
    ```
  </Tabs.Panel>
</Tabs.Root>

# Anatomy

```tsx
import { PreviewCard } from "gnome-ui/preview-card";

<PreviewCard.Root>
  <PreviewCard.Trigger href="..." />
  <PreviewCard.Portal>
    <PreviewCard.Positioner>
      <PreviewCard.Popup>
        {/* free content */}
      </PreviewCard.Popup>
    </PreviewCard.Positioner>
  </PreviewCard.Portal>
</PreviewCard.Root>
```

# Examples

## Multiple Triggers

Use `PreviewCard.createHandle()` and the `payload` prop so multiple links can share a single `PreviewCard.Root` showing different content based on the active trigger.

<Tabs.Root className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm" defaultValue="preview">
  <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
  </Tabs.List>
  <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
    <PreviewCard.PreviewCardMultipleTriggers />
  </Tabs.Panel>
  <Tabs.Panel className="p-0 outline-none" value="code">
    ```tsx
    type RepoPayload = {
      name: string;
      description: string;
      stars: string;
      forks: string;
      watchers: string;
      language: string;
      languageColor: string;
    };

    const repos: Record<string, RepoPayload> = {
      react: {
        name: "facebook/react",
        description: "The library for web and native user interfaces.",
        stars: "224k",
        forks: "45.8k",
        watchers: "6.7k",
        language: "JavaScript",
        languageColor: "#f1e05a",
      },
      // ...
    };

    // Handle creado fuera del componente — se comparte entre triggers
    const multiHandle = PreviewCard.createHandle<RepoPayload>();

    export function PreviewCardMultipleTriggers() {
      return (
        <p>
          Popular projects include{" "}
          <PreviewCard.Trigger
            handle={multiHandle}
            href="https://github.com/facebook/react"
            target="_blank"
            payload={repos.react}
          >
            React
          </PreviewCard.Trigger>
          ,{" "}
          <PreviewCard.Trigger
            handle={multiHandle}
            href="https://github.com/tailwindlabs/tailwindcss"
            target="_blank"
            payload={repos.tailwind}
          >
            Tailwind CSS
          </PreviewCard.Trigger>
          , and{" "}
          <PreviewCard.Trigger
            handle={multiHandle}
            href="https://github.com/mui/base-ui"
            target="_blank"
            payload={repos.baseui}
          >
            Base UI
          </PreviewCard.Trigger>
          .

          {/* Un solo Root para todos los triggers */}
          <PreviewCard.Root handle={multiHandle}>
            {({ payload }) => (
              <PreviewCard.Portal>
                <PreviewCard.Positioner sideOffset={8}>
                  <PreviewCard.Popup>
                    {payload !== undefined && (
                      <div>
                        <p>{payload.name}</p>
                        <p>{payload.description}</p>
                        <div>
                          <span style={{ backgroundColor: payload.languageColor }} />
                          {payload.language}
                          <Icons.Star /> {payload.stars}
                          <Icons.GitFork /> {payload.forks}
                          <Icons.Eye /> {payload.watchers}
                        </div>
                      </div>
                    )}
                  </PreviewCard.Popup>
                </PreviewCard.Positioner>
              </PreviewCard.Portal>
            )}
          </PreviewCard.Root>
        </p>
      );
    }
    ```
  </Tabs.Panel>
</Tabs.Root>

## PreviewCardArrow

Displays an element positioned against the preview card anchor.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Preview Card](https://base-ui.com/react/components/preview-card)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## PreviewCardBackdrop

An overlay displayed beneath the popup.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Preview Card](https://base-ui.com/react/components/preview-card)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## PreviewCardPopup

A container for the preview card contents.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Preview Card](https://base-ui.com/react/components/preview-card)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## PreviewCardPortal

A portal element that moves the popup to a different part of the DOM.
By default, the portal element is appended to `&lt;body&gt;`.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Preview Card](https://base-ui.com/react/components/preview-card)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | `false` | Whether to keep the portal mounted in the DOM while the popup is hidden. |
| **container** | `HTMLElement \| ShadowRoot \| RefObject<HTMLElement \| ShadowRoot>` | — | A parent element to render the portal element into. |


---

## PreviewCardPositioner

Positions the popup against the trigger.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Preview Card](https://base-ui.com/react/components/preview-card)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **anchor** | `Element \| RefObject<Element> \| VirtualElement \| (...) => Element \| VirtualElement)` | — | An element to position the popup against.<br/>By default, the popup will be positioned against the trigger. |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **sticky** | `boolean` | `false` | Whether to maintain the popup in the viewport after<br/>the anchor element was scrolled out of view. |
| **side** | `Side` | `'bottom'` | Which side of the anchor element to align the popup against.<br/>May automatically change to avoid collisions. |
| **align** | `Align` | `'center'` | How to align the popup relative to the specified side. |
| **positionMethod** | `"fixed" \| "absolute"` | `'absolute'` | Determines which CSS `position` property to use. |
| **sideOffset** | `number \| OffsetFunction` | `0` | Distance between the anchor and the popup in pixels.<br/>Also accepts a function that returns the distance to read the dimensions of the anchor<br/>and positioner elements, along with its side and alignment.<br/><br/>The function takes a `data` object parameter with the following properties:<br/>- `data.anchor`: the dimensions of the anchor element with properties `width` and `height`.<br/>- `data.positioner`: the dimensions of the positioner element with properties `width` and `height`.<br/>- `data.side`: which side of the anchor element the positioner is aligned against.<br/>- `data.align`: how the positioner is aligned relative to the specified side.<br/>@example  |
| **alignOffset** | `number \| OffsetFunction` | `0` | Additional offset along the alignment axis in pixels.<br/>Also accepts a function that returns the offset to read the dimensions of the anchor<br/>and positioner elements, along with its side and alignment.<br/><br/>The function takes a `data` object parameter with the following properties:<br/>- `data.anchor`: the dimensions of the anchor element with properties `width` and `height`.<br/>- `data.positioner`: the dimensions of the positioner element with properties `width` and `height`.<br/>- `data.side`: which side of the anchor element the positioner is aligned against.<br/>- `data.align`: how the positioner is aligned relative to the specified side.<br/>@example  |
| **collisionBoundary** | `Boundary` | `'clipping-ancestors'` | An element or a rectangle that delimits the area that the popup is confined to. |
| **collisionPadding** | `Padding` | `5` | Additional space to maintain from the edge of the collision boundary. |
| **arrowPadding** | `number` | `5` | Minimum distance to maintain between the arrow and the edges of the popup.<br/><br/>Use it to prevent the arrow element from hanging out of the rounded corners of a popup. |
| **disableAnchorTracking** | `boolean` | `false` | Whether to disable the popup from tracking any layout shift of its positioning anchor. |
| **collisionAvoidance** | `CollisionAvoidance` | — | Determines how to handle collisions when positioning the popup.<br/>@example  |


---

## PreviewCardRoot

Groups all parts of the preview card.
Doesn’t render its own HTML element.

Documentation: [Base UI Preview Card](https://base-ui.com/react/components/preview-card)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **defaultOpen** | `boolean` | `false` | Whether the preview card is initially open.<br/><br/>To render a controlled preview card, use the `open` prop instead. |
| **open** | `boolean` | — | Whether the preview card is currently open. |
| **onOpenChange** | `(...) => void` | — | Event handler called when the preview card is opened or closed. |
| **onOpenChangeComplete** | `(...) => void` | — | Event handler called after any animations complete when the preview card is opened or closed. |
| **actionsRef** | `RefObject<PreviewCardRootActions>` | — | A ref to imperative actions.<br/>- `unmount`: Unmounts the preview card popup.<br/>- `close`: Closes the preview card imperatively when called. |
| **handle** | `PreviewCardHandle<Payload>` | — | A handle to associate the preview card with a trigger.<br/>If specified, allows external triggers to control the card's open state.<br/>Can be created with the PreviewCard.createHandle() method. |
| **children** | `ReactNode \| PayloadChildRenderFunction<Payload>` | — | The content of the preview card.<br/>This can be a regular React node or a render function that receives the `payload` of the active trigger. |
| **triggerId** | `string` | — | ID of the trigger that the preview card is associated with.<br/>This is useful in conjuntion with the `open` prop to create a controlled preview card.<br/>There's no need to specify this prop when the preview card is uncontrolled (i.e. when the `open` prop is not set). |
| **defaultTriggerId** | `string` | — | ID of the trigger that the preview card is associated with.<br/>This is useful in conjunction with the `defaultOpen` prop to create an initially open preview card. |


---

## PreviewCardTrigger

A link that opens the preview card.
Renders an `&lt;a&gt;` element.

Documentation: [Base UI Preview Card](https://base-ui.com/react/components/preview-card)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **handle** | `PreviewCardHandle<Payload>` | — | A handle to associate the trigger with a preview card. |
| **payload** | `Payload` | — | A payload to pass to the preview card when it is opened. |
| **delay** | `number` | `600` | How long to wait before the preview card opens. Specified in milliseconds. |
| **closeDelay** | `number` | `300` | How long to wait before closing the preview card. Specified in milliseconds. |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## PreviewCardViewport

A viewport for displaying content transitions.
This component is only required if one popup can be opened by multiple triggers, its content change based on the trigger
and switching between them is animated.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Preview Card](https://base-ui.com/react/components/preview-card)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **children** | `ReactNode` | — | The content to render inside the transition container. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

