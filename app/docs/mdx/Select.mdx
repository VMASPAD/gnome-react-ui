---
title: Select
---

# Select

## Examples

### Example 1 — Basic select

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="code">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
import * as Select from './select/index.parts';

export default function BasicSelect() {
  return (
    <Select.Root defaultValue="react">
      <Select.Trigger className="inline-flex items-center justify-between gap-2 px-3 py-2 rounded-md border border-gray-200 text-sm bg-white w-40 hover:bg-gray-50">
        <Select.Value placeholder="Choose…" />
        <Select.Icon className="text-gray-400">▾</Select.Icon>
      </Select.Trigger>
      <Select.Portal>
        <Select.Positioner sideOffset={4}>
          <Select.Popup className="w-40 rounded-lg border border-gray-200 bg-white py-1 shadow-lg">
            <Select.Item value="react" className="px-3 py-2 text-sm cursor-pointer hover:bg-gray-100 data-[highlighted]:bg-gray-100">
              <Select.ItemText>React</Select.ItemText>
            </Select.Item>
            <Select.Item value="vue" className="px-3 py-2 text-sm cursor-pointer hover:bg-gray-100 data-[highlighted]:bg-gray-100">
              <Select.ItemText>Vue</Select.ItemText>
            </Select.Item>
            <Select.Item value="svelte" className="px-3 py-2 text-sm cursor-pointer hover:bg-gray-100 data-[highlighted]:bg-gray-100">
              <Select.ItemText>Svelte</Select.ItemText>
            </Select.Item>
          </Select.Popup>
        </Select.Positioner>
      </Select.Portal>
    </Select.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Select.Root defaultValue="react">
          <Select.Trigger className="inline-flex items-center justify-between gap-2 px-3 py-2 rounded-md border border-gray-200 text-sm bg-white w-40 hover:bg-gray-50">
            <Select.Value placeholder="Choose…" />
            <Select.Icon className="text-gray-400">▾</Select.Icon>
          </Select.Trigger>
          <Select.Portal>
            <Select.Positioner sideOffset={4}>
              <Select.Popup className="w-40 rounded-lg border border-gray-200 bg-white py-1 shadow-lg">
                <Select.Item value="react" className="px-3 py-2 text-sm cursor-pointer hover:bg-gray-100 data-[highlighted]:bg-gray-100"><Select.ItemText>React</Select.ItemText></Select.Item>
                <Select.Item value="vue" className="px-3 py-2 text-sm cursor-pointer hover:bg-gray-100 data-[highlighted]:bg-gray-100"><Select.ItemText>Vue</Select.ItemText></Select.Item>
                <Select.Item value="svelte" className="px-3 py-2 text-sm cursor-pointer hover:bg-gray-100 data-[highlighted]:bg-gray-100"><Select.ItemText>Svelte</Select.ItemText></Select.Item>
              </Select.Popup>
            </Select.Positioner>
          </Select.Portal>
        </Select.Root>
    </Tabs.Panel>
</Tabs.Root>

### Example 2 — Select with groups

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="code">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
export default function GroupedSelect() {
  return (
    <Select.Root>
      <Select.Trigger className="inline-flex items-center justify-between gap-2 px-3 py-2 rounded-md border border-gray-200 text-sm bg-white w-48 hover:bg-gray-50">
        <Select.Value placeholder="Select country…" />
        <Select.Icon className="text-gray-400">▾</Select.Icon>
      </Select.Trigger>
      <Select.Portal>
        <Select.Positioner sideOffset={4}>
          <Select.Popup className="w-48 rounded-lg border border-gray-200 bg-white py-1 shadow-lg max-h-60 overflow-auto">
            <Select.Group>
              <Select.GroupLabel className="px-3 py-1.5 text-[11px] font-semibold uppercase text-gray-400 tracking-wider">Europe</Select.GroupLabel>
              <Select.Item value="de" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>Germany</Select.ItemText></Select.Item>
              <Select.Item value="fr" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>France</Select.ItemText></Select.Item>
              <Select.Item value="es" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>Spain</Select.ItemText></Select.Item>
            </Select.Group>
            <Select.Separator className="my-1 border-t border-gray-100" />
            <Select.Group>
              <Select.GroupLabel className="px-3 py-1.5 text-[11px] font-semibold uppercase text-gray-400 tracking-wider">Americas</Select.GroupLabel>
              <Select.Item value="us" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>United States</Select.ItemText></Select.Item>
              <Select.Item value="ca" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>Canada</Select.ItemText></Select.Item>
            </Select.Group>
          </Select.Popup>
        </Select.Positioner>
      </Select.Portal>
    </Select.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Select.Root>
          <Select.Trigger className="inline-flex items-center justify-between gap-2 px-3 py-2 rounded-md border border-gray-200 text-sm bg-white w-48 hover:bg-gray-50">
            <Select.Value placeholder="Select country…" />
            <Select.Icon className="text-gray-400">▾</Select.Icon>
          </Select.Trigger>
          <Select.Portal>
            <Select.Positioner sideOffset={4}>
              <Select.Popup className="w-48 rounded-lg border border-gray-200 bg-white py-1 shadow-lg max-h-60 overflow-auto">
                <Select.Group>
                  <Select.GroupLabel className="px-3 py-1.5 text-[11px] font-semibold uppercase text-gray-400 tracking-wider">Europe</Select.GroupLabel>
                  <Select.Item value="de" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>Germany</Select.ItemText></Select.Item>
                  <Select.Item value="fr" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>France</Select.ItemText></Select.Item>
                  <Select.Item value="es" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>Spain</Select.ItemText></Select.Item>
                </Select.Group>
                <Select.Separator className="my-1 border-t border-gray-100" />
                <Select.Group>
                  <Select.GroupLabel className="px-3 py-1.5 text-[11px] font-semibold uppercase text-gray-400 tracking-wider">Americas</Select.GroupLabel>
                  <Select.Item value="us" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>United States</Select.ItemText></Select.Item>
                  <Select.Item value="ca" className="px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>Canada</Select.ItemText></Select.Item>
                </Select.Group>
              </Select.Popup>
            </Select.Positioner>
          </Select.Portal>
        </Select.Root>
    </Tabs.Panel>
</Tabs.Root>

### Example 3 — Select with item indicator

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="code">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
export default function SelectWithIndicator() {
  return (
    <Select.Root defaultValue="dark">
      <Select.Trigger className="inline-flex items-center justify-between gap-2 px-3 py-2 rounded-md border border-gray-200 text-sm bg-white w-40 hover:bg-gray-50">
        <Select.Value />
        <Select.Icon className="text-gray-400">▾</Select.Icon>
      </Select.Trigger>
      <Select.Portal>
        <Select.Positioner sideOffset={4}>
          <Select.Popup className="w-40 rounded-lg border border-gray-200 bg-white py-1 shadow-lg">
            {['Light', 'Dark', 'System'].map((theme) => (
              <Select.Item key={theme} value={theme.toLowerCase()} className="flex items-center justify-between px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100">
                <Select.ItemText>{theme}</Select.ItemText>
                <Select.ItemIndicator className="text-blue-600 text-xs">✓</Select.ItemIndicator>
              </Select.Item>
            ))}
          </Select.Popup>
        </Select.Positioner>
      </Select.Portal>
    </Select.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Select.Root defaultValue="dark">
          <Select.Trigger className="inline-flex items-center justify-between gap-2 px-3 py-2 rounded-md border border-gray-200 text-sm bg-white w-40 hover:bg-gray-50">
            <Select.Value />
            <Select.Icon className="text-gray-400">▾</Select.Icon>
          </Select.Trigger>
          <Select.Portal>
            <Select.Positioner sideOffset={4}>
              <Select.Popup className="w-40 rounded-lg border border-gray-200 bg-white py-1 shadow-lg">
                <Select.Item value="light" className="flex items-center justify-between px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>Light</Select.ItemText><Select.ItemIndicator className="text-blue-600 text-xs">✓</Select.ItemIndicator></Select.Item>
                <Select.Item value="dark" className="flex items-center justify-between px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>Dark</Select.ItemText><Select.ItemIndicator className="text-blue-600 text-xs">✓</Select.ItemIndicator></Select.Item>
                <Select.Item value="system" className="flex items-center justify-between px-3 py-2 text-sm cursor-pointer data-[highlighted]:bg-gray-100"><Select.ItemText>System</Select.ItemText><Select.ItemIndicator className="text-blue-600 text-xs">✓</Select.ItemIndicator></Select.Item>
              </Select.Popup>
            </Select.Positioner>
          </Select.Portal>
        </Select.Root>
    </Tabs.Panel>
</Tabs.Root>

## SelectArrow

Displays an element positioned against the select popup anchor.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## SelectBackdrop

An overlay displayed beneath the menu popup.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## SelectGroup

Groups related select items with the corresponding label.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## SelectGroupLabel

An accessible label that is automatically associated with its parent group.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## SelectIcon

An icon that indicates that the trigger button opens a select popup.
Renders a `&lt;span&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## SelectItem

An individual option in the select popup.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **label** | `string` | — | Specifies the text label to use when the item is matched during keyboard text navigation.<br/><br/>Defaults to the item text content if not provided. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **value** | `any` | `null` | A unique value that identifies this select item. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **nativeButton** | `boolean` | `false` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `true` if the rendered element is a native button. |


---

## SelectItemIndicator

Indicates whether the select item is selected.
Renders a `&lt;span&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | — | Whether to keep the HTML element in the DOM when the item is not selected. |


---

## SelectItemText

A text label of the select item.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## SelectList

A container for the select items.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## SelectPopup

A container for the select list.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **finalFocus** | `boolean \| RefObject<HTMLElement> \| (...) => boolean \| void \| HTMLElement)` | — | Determines the element to focus when the select popup is closed.<br/><br/>- `false`: Do not move focus.<br/>- `true`: Move focus based on the default behavior (trigger or previously focused element).<br/>- `RefObject`: Move focus to the ref element.<br/>- `function`: Called with the interaction type (`mouse`, `touch`, `pen`, or `keyboard`).<br/>  Return an element to focus, `true` to use the default behavior, or `false`/`undefined` to do nothing. |


---

## SelectPortal

A portal element that moves the popup to a different part of the DOM.
By default, the portal element is appended to `&lt;body&gt;`.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **container** | `HTMLElement \| ShadowRoot \| RefObject<HTMLElement \| ShadowRoot>` | — | A parent element to render the portal element into. |


---

## SelectPositioner

Positions the select popup.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **anchor** | `Element \| RefObject<Element> \| VirtualElement \| (...) => Element \| VirtualElement)` | — | An element to position the popup against.<br/>By default, the popup will be positioned against the trigger. |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **sticky** | `boolean` | `false` | Whether to maintain the popup in the viewport after<br/>the anchor element was scrolled out of view. |
| **side** | `Side` | `'bottom'` | Which side of the anchor element to align the popup against.<br/>May automatically change to avoid collisions. |
| **align** | `Align` | `'center'` | How to align the popup relative to the specified side. |
| **positionMethod** | `"fixed" \| "absolute"` | `'absolute'` | Determines which CSS `position` property to use. |
| **sideOffset** | `number \| OffsetFunction` | `0` | Distance between the anchor and the popup in pixels.<br/>Also accepts a function that returns the distance to read the dimensions of the anchor<br/>and positioner elements, along with its side and alignment.<br/><br/>The function takes a `data` object parameter with the following properties:<br/>- `data.anchor`: the dimensions of the anchor element with properties `width` and `height`.<br/>- `data.positioner`: the dimensions of the positioner element with properties `width` and `height`.<br/>- `data.side`: which side of the anchor element the positioner is aligned against.<br/>- `data.align`: how the positioner is aligned relative to the specified side.<br/>@example  |
| **alignOffset** | `number \| OffsetFunction` | `0` | Additional offset along the alignment axis in pixels.<br/>Also accepts a function that returns the offset to read the dimensions of the anchor<br/>and positioner elements, along with its side and alignment.<br/><br/>The function takes a `data` object parameter with the following properties:<br/>- `data.anchor`: the dimensions of the anchor element with properties `width` and `height`.<br/>- `data.positioner`: the dimensions of the positioner element with properties `width` and `height`.<br/>- `data.side`: which side of the anchor element the positioner is aligned against.<br/>- `data.align`: how the positioner is aligned relative to the specified side.<br/>@example  |
| **collisionBoundary** | `Boundary` | `'clipping-ancestors'` | An element or a rectangle that delimits the area that the popup is confined to. |
| **collisionPadding** | `Padding` | `5` | Additional space to maintain from the edge of the collision boundary. |
| **arrowPadding** | `number` | `5` | Minimum distance to maintain between the arrow and the edges of the popup.<br/><br/>Use it to prevent the arrow element from hanging out of the rounded corners of a popup. |
| **disableAnchorTracking** | `boolean` | `false` | Whether to disable the popup from tracking any layout shift of its positioning anchor. |
| **collisionAvoidance** | `CollisionAvoidance` | — | Determines how to handle collisions when positioning the popup.<br/>@example  |
| **alignItemWithTrigger** | `boolean` | `true` | Whether the positioner overlaps the trigger so the selected item's text is aligned with the trigger's value text. This only applies to mouse input and is automatically disabled if there is not enough space. |


---

## SelectRoot

Groups all parts of the select.
Doesn’t render its own HTML element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **inputRef** | `Ref<HTMLInputElement>` | — | A ref to access the hidden input element. |
| **name** | `string` | — | Identifies the field when a form is submitted. |
| **autoComplete** | `string` | — | Provides a hint to the browser for autofill.<br/>@see https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Attributes/autocomplete |
| **id** | `string` | — | The id of the Select. |
| **required** | `boolean` | `false` | Whether the user must choose a value before submitting a form. |
| **readOnly** | `boolean` | `false` | Whether the user should be unable to choose a different option from the select popup. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **multiple** | `boolean` | `false` | Whether multiple items can be selected. |
| **highlightItemOnHover** | `boolean` | `true` | Whether moving the pointer over items should highlight them.<br/>Disabling this prop allows CSS `:hover` to be differentiated from the `:focus` (`data-highlighted`) state. |
| **defaultOpen** | `boolean` | `false` | Whether the select popup is initially open.<br/><br/>To render a controlled select popup, use the `open` prop instead. |
| **onOpenChange** | `(...) => void` | — | Event handler called when the select popup is opened or closed. |
| **onOpenChangeComplete** | `(...) => void` | — | Event handler called after any animations complete when the select popup is opened or closed. |
| **open** | `boolean` | — | Whether the select popup is currently open. |
| **modal** | `boolean` | `true` | Determines if the select enters a modal state when open.<br/>- `true`: user interaction is limited to the select: document page scroll is locked and pointer interactions on outside elements are disabled.<br/>- `false`: user interaction with the rest of the document is allowed. |
| **actionsRef** | `RefObject<SelectRootActions>` | — | A ref to imperative actions.<br/>- `unmount`: When specified, the select will not be unmounted when closed.<br/>Instead, the `unmount` function must be called to unmount the select manually.<br/>Useful when the select's animation is controlled by an external library. |
| **items** | `Record<string, ReactNode> \| readonly { label: ReactNode; value: any; }[]` | — | Data structure of the items rendered in the select popup.<br/>When specified, `&lt;Select.Value&gt;` renders the label of the selected item instead of the raw value.<br/>@example  |
| **itemToStringLabel** | `(...) => string` | — | When the item values are objects (`&lt;Select.Item value=&#123;object&#125;&gt;`), this function converts the object value to a string representation for display in the trigger.<br/>If the shape of the object is `&#123; value, label &#125;`, the label will be used automatically without needing to specify this prop. |
| **itemToStringValue** | `(...) => string` | — | When the item values are objects (`&lt;Select.Item value=&#123;object&#125;&gt;`), this function converts the object value to a string representation for form submission.<br/>If the shape of the object is `&#123; value, label &#125;`, the value will be used automatically without needing to specify this prop. |
| **isItemEqualToValue** | `(...) => boolean` | — | Custom comparison logic used to determine if a select item value matches the current selected value. Useful when item values are objects without matching referentially.<br/>Defaults to `Object.is` comparison. |
| **defaultValue** | `SelectValueType<Value, Multiple>` | — | The uncontrolled value of the select when it’s initially rendered.<br/><br/>To render a controlled select, use the `value` prop instead. |
| **value** | `SelectValueType<Value, Multiple>` | — | The value of the select. Use when controlled. |
| **onValueChange** | `(...) => void` | — | Event handler called when the value of the select changes. |


---

## SelectScrollArrow



### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | `false` | Whether to keep the HTML element in the DOM while the select popup is not scrollable. |
| **direction*** | `"up" \| "down"` | — | — |


---

## SelectScrollDownArrow

An element that scrolls the select popup down when hovered. Does not render when using touch input.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | `false` | Whether to keep the HTML element in the DOM while the select popup is not scrollable. |


---

## SelectScrollUpArrow

An element that scrolls the select popup up when hovered. Does not render when using touch input.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | `false` | Whether to keep the HTML element in the DOM while the select popup is not scrollable. |


---

## SelectTrigger

A button that opens the select popup.
Renders a `&lt;button&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **disabled** | `boolean` | — | Whether the component should ignore user interaction. |
| **nativeButton** | `boolean` | `true` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `false` if the rendered element is not a button (e.g. `&lt;div&gt;`). |


---

## SelectValue

A text label of the currently selected item.
Renders a `&lt;span&gt;` element.

Documentation: [Base UI Select](https://base-ui.com/react/components/select)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **children** | `ReactNode \| (...) => ReactNode)` | — | Accepts a function that returns a `ReactNode` to format the selected value.<br/>@example  |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **placeholder** | `ReactNode` | — | The placeholder value to display when no value is selected.<br/>This is overridden by `children` if specified, or by a null item's label in `items`. |


---

