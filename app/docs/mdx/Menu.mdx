---
title: Menu
---

# Menu

## MenuArrow

Displays an element positioned against the menu anchor.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## MenuBackdrop

An overlay displayed beneath the menu popup.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## MenuCheckboxItem

A menu item that toggles a setting on or off.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **defaultChecked** | `boolean` | `false` | Whether the checkbox item is initially ticked.<br/><br/>To render a controlled checkbox item, use the `checked` prop instead. |
| **label** | `string` | — | Overrides the text label to use when the item is matched during keyboard text navigation. |
| **id** | `string` | — | @ignore |
| **onClick** | `(...) => void` | — | The click handler for the menu item. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **nativeButton** | `boolean` | `false` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `true` if the rendered element is a native button. |
| **checked** | `boolean` | — | Whether the checkbox item is currently ticked.<br/><br/>To render an uncontrolled checkbox item, use the `defaultChecked` prop instead. |
| **onCheckedChange** | `(...) => void` | — | Event handler called when the checkbox item is ticked or unticked. |
| **closeOnClick** | `boolean` | `false` | Whether to close the menu when the item is clicked. |


---

## MenuCheckboxItemIndicator

Indicates whether the checkbox item is ticked.
Renders a `&lt;span&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | `false` | Whether to keep the HTML element in the DOM when the checkbox item is not checked. |


---

## MenuGroup

Groups related menu items with the corresponding label.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **children** | `ReactNode` | — | The content of the component. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## MenuGroupLabel

An accessible label that is automatically associated with its parent group.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## MenuItem

An individual interactive item in the menu.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **label** | `string` | — | Overrides the text label to use when the item is matched during keyboard text navigation. |
| **id** | `string` | — | @ignore |
| **onClick** | `(...) => void` | — | The click handler for the menu item. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **nativeButton** | `boolean` | `false` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `true` if the rendered element is a native button. |
| **closeOnClick** | `boolean` | `true` | Whether to close the menu when the item is clicked. |


---

## MenuLinkItem

A link in the menu that can be used to navigate to a different page or section.
Renders an `&lt;a&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **label** | `string` | — | Overrides the text label to use when the item is matched during keyboard text navigation. |
| **id** | `string` | — | @ignore |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **closeOnClick** | `boolean` | `false` | Whether to close the menu when the item is clicked. |


---

## MenuPopup

A container for the menu items.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **id** | `string` | — | @ignore |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **finalFocus** | `boolean \| RefObject<HTMLElement> \| (...) => boolean \| void \| HTMLElement)` | — | Determines the element to focus when the menu is closed.<br/><br/>- `false`: Do not move focus.<br/>- `true`: Move focus based on the default behavior (trigger or previously focused element).<br/>- `RefObject`: Move focus to the ref element.<br/>- `function`: Called with the interaction type (`mouse`, `touch`, `pen`, or `keyboard`).<br/>  Return an element to focus, `true` to use the default behavior, or `false`/`undefined` to do nothing. |


---

## MenuPortal

A portal element that moves the popup to a different part of the DOM.
By default, the portal element is appended to `&lt;body&gt;`.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | `false` | Whether to keep the portal mounted in the DOM while the popup is hidden. |
| **container** | `HTMLElement \| ShadowRoot \| RefObject<HTMLElement \| ShadowRoot>` | — | A parent element to render the portal element into. |


---

## MenuPositioner

Positions the menu popup against the trigger.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **anchor** | `Element \| RefObject<Element> \| VirtualElement \| (...) => Element \| VirtualElement)` | — | An element to position the popup against.<br/>By default, the popup will be positioned against the trigger. |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **sticky** | `boolean` | `false` | Whether to maintain the popup in the viewport after<br/>the anchor element was scrolled out of view. |
| **side** | `Side` | `'bottom'` | Which side of the anchor element to align the popup against.<br/>May automatically change to avoid collisions. |
| **align** | `Align` | `'center'` | How to align the popup relative to the specified side. |
| **positionMethod** | `"fixed" \| "absolute"` | `'absolute'` | Determines which CSS `position` property to use. |
| **sideOffset** | `number \| OffsetFunction` | `0` | Distance between the anchor and the popup in pixels.<br/>Also accepts a function that returns the distance to read the dimensions of the anchor<br/>and positioner elements, along with its side and alignment.<br/><br/>The function takes a `data` object parameter with the following properties:<br/>- `data.anchor`: the dimensions of the anchor element with properties `width` and `height`.<br/>- `data.positioner`: the dimensions of the positioner element with properties `width` and `height`.<br/>- `data.side`: which side of the anchor element the positioner is aligned against.<br/>- `data.align`: how the positioner is aligned relative to the specified side.<br/>@example  |
| **alignOffset** | `number \| OffsetFunction` | `0` | Additional offset along the alignment axis in pixels.<br/>Also accepts a function that returns the offset to read the dimensions of the anchor<br/>and positioner elements, along with its side and alignment.<br/><br/>The function takes a `data` object parameter with the following properties:<br/>- `data.anchor`: the dimensions of the anchor element with properties `width` and `height`.<br/>- `data.positioner`: the dimensions of the positioner element with properties `width` and `height`.<br/>- `data.side`: which side of the anchor element the positioner is aligned against.<br/>- `data.align`: how the positioner is aligned relative to the specified side.<br/>@example  |
| **collisionBoundary** | `Boundary` | `'clipping-ancestors'` | An element or a rectangle that delimits the area that the popup is confined to. |
| **collisionPadding** | `Padding` | `5` | Additional space to maintain from the edge of the collision boundary. |
| **arrowPadding** | `number` | `5` | Minimum distance to maintain between the arrow and the edges of the popup.<br/><br/>Use it to prevent the arrow element from hanging out of the rounded corners of a popup. |
| **disableAnchorTracking** | `boolean` | `false` | Whether to disable the popup from tracking any layout shift of its positioning anchor. |
| **collisionAvoidance** | `CollisionAvoidance` | — | Determines how to handle collisions when positioning the popup.<br/>@example  |


---

## MenuRadioGroup

Groups related radio items.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **defaultValue** | `any` | — | The uncontrolled value of the radio item that should be initially selected.<br/><br/>To render a controlled radio group, use the `value` prop instead. |
| **children** | `ReactNode` | — | The content of the component. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **value** | `any` | — | The controlled value of the radio item that should be currently selected.<br/><br/>To render an uncontrolled radio group, use the `defaultValue` prop instead. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **onValueChange** | `(...) => void` | — | Function called when the selected value changes. |


---

## MenuRadioItem

A menu item that works like a radio button in a given group.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **label** | `string` | — | Overrides the text label to use when the item is matched during keyboard text navigation. |
| **id** | `string` | — | @ignore |
| **onClick** | `(...) => void` | — | The click handler for the menu item. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **value*** | `any` | — | Value of the radio item.<br/>This is the value that will be set in the MenuRadioGroup when the item is selected. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **nativeButton** | `boolean` | `false` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `true` if the rendered element is a native button. |
| **closeOnClick** | `boolean` | `false` | Whether to close the menu when the item is clicked. |


---

## MenuRadioItemIndicator

Indicates whether the radio item is selected.
Renders a `&lt;span&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | `false` | Whether to keep the HTML element in the DOM when the radio item is inactive. |


---

## MenuRoot

Groups all parts of the menu.
Doesn’t render its own HTML element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

No props available.

---

## MenuSubmenuRoot

Groups all parts of a submenu.
Doesn’t render its own HTML element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **onOpenChange** | `(...) => void` | — | Event handler called when the menu is opened or closed. |
| **closeParentOnEsc** | `boolean` | `false` | When in a submenu, determines whether pressing the Escape key<br/>closes the entire menu, or only the current child menu. |
| **children** | `ReactNode \| PayloadChildRenderFunction<unknown>` | — | The content of the popover.<br/>This can be a regular React node or a render function that receives the `payload` of the active trigger. |
| **open** | `boolean` | — | Whether the menu is currently open. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **orientation** | `MenuRootOrientation` | `'vertical'` | The visual orientation of the menu.<br/>Controls whether roving focus uses up/down or left/right arrow keys. |
| **triggerId** | `string` | — | ID of the trigger that the popover is associated with.<br/>This is useful in conjunction with the `open` prop to create a controlled popover.<br/>There's no need to specify this prop when the popover is uncontrolled (i.e. when the `open` prop is not set). |
| **loopFocus** | `boolean` | `true` | Whether to loop keyboard focus back to the first item<br/>when the end of the list is reached while using the arrow keys. |
| **actionsRef** | `RefObject<MenuRootActions>` | — | A ref to imperative actions.<br/>- `unmount`: When specified, the menu will not be unmounted when closed.<br/>   Instead, the `unmount` function must be called to unmount the menu manually.<br/>  Useful when the menu's animation is controlled by an external library.<br/>- `close`: When specified, the menu can be closed imperatively. |
| **handle** | `MenuHandle<unknown>` | — | A handle to associate the menu with a trigger.<br/>If specified, allows external triggers to control the menu's open state. |
| **defaultOpen** | `boolean` | `false` | Whether the menu is initially open.<br/><br/>To render a controlled menu, use the `open` prop instead. |
| **onOpenChangeComplete** | `(...) => void` | — | Event handler called after any animations complete when the menu is closed. |
| **defaultTriggerId** | `string` | — | ID of the trigger that the popover is associated with.<br/>This is useful in conjunction with the `defaultOpen` prop to create an initially open popover. |
| **highlightItemOnHover** | `boolean` | `true` | Whether moving the pointer over items should highlight them.<br/>Disabling this prop allows CSS `:hover` to be differentiated from the `:focus` (`data-highlighted`) state. |


---

## MenuSubmenuTrigger

A menu item that opens a submenu.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **label** | `string` | — | Overrides the text label to use when the item is matched during keyboard text navigation. |
| **id** | `string` | — | @ignore |
| **onClick** | `(...) => void` | — | — |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **nativeButton** | `boolean` | `false` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `true` if the rendered element is a native button. |
| **delay** | `number` | `100` | How long to wait before the menu may be opened on hover. Specified in milliseconds.<br/><br/>Requires the `openOnHover` prop. |
| **openOnHover** | `boolean` | — | Whether the menu should also open when the trigger is hovered. |
| **closeDelay** | `number` | `0` | How long to wait before closing the menu that was opened on hover.<br/>Specified in milliseconds.<br/><br/>Requires the `openOnHover` prop. |


---

## MenuTrigger

A button that opens the menu.
Renders a `&lt;button&gt;` element.

Documentation: [Base UI Menu](https://base-ui.com/react/components/menu)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **handle** | `MenuHandle<Payload>` | — | A handle to associate the trigger with a menu. |
| **payload** | `Payload` | — | A payload to pass to the menu when it is opened. |
| **delay** | `number` | `100` | How long to wait before the menu may be opened on hover. Specified in milliseconds.<br/><br/>Requires the `openOnHover` prop. |
| **closeDelay** | `number` | `0` | How long to wait before closing the menu that was opened on hover.<br/>Specified in milliseconds.<br/><br/>Requires the `openOnHover` prop. |
| **openOnHover** | `boolean` | — | Whether the menu should also open when the trigger is hovered. |
| **nativeButton** | `boolean` | `true` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `false` if the rendered element is not a button (e.g. `&lt;div&gt;`). |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

