---
title: Collapsible
---

# Collapsible

## Examples

### Example 1 — Basic collapsible

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="code">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
import * as Collapsible from './collapsible/index.parts';

export default function BasicCollapsible() {
  return (
    <Collapsible.Root className="w-72 rounded-lg border border-gray-200">
      <Collapsible.Trigger className="flex w-full items-center justify-between px-4 py-3 text-sm font-medium hover:bg-gray-50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-500">
        Show details
        <span className="text-gray-400 data-[panel-open]:rotate-180 transition-transform">▼</span>
      </Collapsible.Trigger>
      <Collapsible.Panel className="px-4 pb-4 text-sm text-gray-600">
        <p>This content is revealed when the trigger is clicked. Use the Collapsible component to progressively disclose information.</p>
      </Collapsible.Panel>
    </Collapsible.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Collapsible.Root className="w-72 rounded-lg border border-gray-200">
          <Collapsible.Trigger className="flex w-full items-center justify-between px-4 py-3 text-sm font-medium hover:bg-gray-50">
            Show details
            <Icons.ChevronDown size={14} className="text-gray-400" />
          </Collapsible.Trigger>
          <Collapsible.Panel className="px-4 pb-4 text-sm text-gray-600">
            <p>This content is revealed when the trigger is clicked.</p>
          </Collapsible.Panel>
        </Collapsible.Root>
    </Tabs.Panel>
</Tabs.Root>

### Example 2 — Animated panel height

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="code">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
export default function AnimatedCollapsible() {
  return (
    <Collapsible.Root className="w-72 rounded-xl border border-gray-200 bg-white shadow-sm">
      <Collapsible.Trigger className="flex w-full items-center justify-between px-5 py-3.5 text-sm font-semibold text-gray-800 hover:bg-gray-50 rounded-xl data-[panel-open]:rounded-b-none transition-colors">
        FAQ: How does billing work?
        <span className="text-gray-400 transition-transform data-[panel-open]:rotate-45">+</span>
      </Collapsible.Trigger>
      <Collapsible.Panel className="overflow-hidden h-[var(--collapsible-panel-height)] transition-[height] duration-300 ease-out data-[starting-style]:h-0 data-[ending-style]:h-0">
        <p className="px-5 pb-4 pt-2 text-sm text-gray-600 leading-relaxed">
          You are billed monthly based on usage. All plans include a 14-day free trial and you can cancel at any time.
        </p>
      </Collapsible.Panel>
    </Collapsible.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Collapsible.Root className="w-72 rounded-xl border border-gray-200 bg-white shadow-sm">
          <Collapsible.Trigger className="flex w-full items-center justify-between px-5 py-3.5 text-sm font-semibold text-gray-800 hover:bg-gray-50 rounded-xl transition-colors">
            FAQ: How does billing work?
            <Icons.Plus size={14} className="text-gray-400" />
          </Collapsible.Trigger>
          <Collapsible.Panel className="overflow-hidden h-[var(--collapsible-panel-height)] transition-[height] duration-300 ease-out data-[starting-style]:h-0 data-[ending-style]:h-0">
            <p className="px-5 pb-4 pt-2 text-sm text-gray-600 leading-relaxed">You are billed monthly based on usage. All plans include a 14-day free trial.</p>
          </Collapsible.Panel>
        </Collapsible.Root>
    </Tabs.Panel>
</Tabs.Root>

### Example 3 — `keepMounted` (panel stays in DOM)

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="code">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
// keepMounted keeps the panel in the DOM even when collapsed.
// Useful for preserving form state or avoiding re-renders.
export default function KeepMountedCollapsible() {
  return (
    <Collapsible.Root defaultOpen className="w-72 rounded-lg border border-gray-200">
      <Collapsible.Trigger className="flex w-full items-center justify-between px-4 py-3 text-sm font-medium hover:bg-gray-50">
        Advanced options
        <span className="text-xs text-gray-400">(keepMounted)</span>
      </Collapsible.Trigger>
      <Collapsible.Panel keepMounted className="px-4 pb-4">
        <label className="flex flex-col gap-1">
          <span className="text-xs font-medium text-gray-600">API Endpoint</span>
          <input className="w-full rounded border border-gray-200 px-3 py-1.5 text-sm" defaultValue="https://api.example.com" />
        </label>
      </Collapsible.Panel>
    </Collapsible.Root>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Collapsible.Root defaultOpen className="w-72 rounded-lg border border-gray-200">
          <Collapsible.Trigger className="flex w-full items-center justify-between px-4 py-3 text-sm font-medium hover:bg-gray-50">
            Advanced options
            <Icons.ChevronDown size={14} className="text-gray-400" />
          </Collapsible.Trigger>
          <Collapsible.Panel keepMounted className="px-4 pb-4">
            <label className="flex flex-col gap-1">
              <span className="text-xs font-medium text-gray-600">API Endpoint</span>
              <input className="w-full rounded border border-gray-200 px-3 py-1.5 text-sm" defaultValue="https://api.example.com" />
            </label>
          </Collapsible.Panel>
        </Collapsible.Root>
    </Tabs.Panel>
</Tabs.Root>

## CollapsiblePanel

A panel with the collapsible contents.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Collapsible](https://base-ui.com/react/components/collapsible)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **hiddenUntilFound** | `boolean` | `false` | Allows the browser’s built-in page search to find and expand the panel contents.<br/><br/>Overrides the `keepMounted` prop and uses `hidden="until-found"`<br/>to hide the element without removing it from the DOM. |
| **keepMounted** | `boolean` | `false` | Whether to keep the element in the DOM while the panel is hidden.<br/>This prop is ignored when `hiddenUntilFound` is used. |


---

## CollapsibleRoot

Groups all parts of the collapsible.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Collapsible](https://base-ui.com/react/components/collapsible)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **open** | `boolean` | — | Whether the collapsible panel is currently open.<br/><br/>To render an uncontrolled collapsible, use the `defaultOpen` prop instead. |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **onOpenChange** | `(...) => void` | — | Event handler called when the panel is opened or closed. |
| **defaultOpen** | `boolean` | `false` | Whether the collapsible panel is initially open.<br/><br/>To render a controlled collapsible, use the `open` prop instead. |


---

## CollapsibleTrigger

A button that opens and closes the collapsible panel.
Renders a `&lt;button&gt;` element.

Documentation: [Base UI Collapsible](https://base-ui.com/react/components/collapsible)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **nativeButton** | `boolean` | `true` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `false` if the rendered element is not a button (e.g. `&lt;div&gt;`). |


---

