---
title: Radio
---

# Radio

## Examples

### Example 1 — Basic radio group

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="preview">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
import * as Radio from './radio/index.parts';

export default function BasicRadioGroup() {
  return (
    <Radio.Group defaultValue="option1" className="flex flex-col gap-2">
      {['Option A', 'Option B', 'Option C'].map((label, i) => (
        <Radio.Root key={label} value={`option${i + 1}`} className="flex items-center gap-2 cursor-pointer">
          <Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center data-[checked]:border-blue-600">
            <span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" />
          </Radio.Indicator>
          <span className="text-sm text-gray-700">{label}</span>
        </Radio.Root>
      ))}
    </Radio.Group>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Radio.Group defaultValue="option1" className="flex flex-col gap-2">
          <Radio.Root value="option1" className="flex items-center gap-2 cursor-pointer">
            <Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center data-[checked]:border-blue-600"><span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" /></Radio.Indicator>
            <span className="text-sm text-gray-700">Option A</span>
          </Radio.Root>
          <Radio.Root value="option2" className="flex items-center gap-2 cursor-pointer">
            <Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center data-[checked]:border-blue-600"><span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" /></Radio.Indicator>
            <span className="text-sm text-gray-700">Option B</span>
          </Radio.Root>
          <Radio.Root value="option3" className="flex items-center gap-2 cursor-pointer">
            <Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center data-[checked]:border-blue-600"><span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" /></Radio.Indicator>
            <span className="text-sm text-gray-700">Option C</span>
          </Radio.Root>
        </Radio.Group>
    </Tabs.Panel>
</Tabs.Root>

### Example 2 — Card-style radio group

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="preview">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
const plans = [
  { value: 'free', label: 'Free', description: 'Up to 3 projects' },
  { value: 'pro', label: 'Pro', description: '$9/mo, unlimited' },
  { value: 'team', label: 'Team', description: '$29/mo, 5 seats' },
];

export default function CardRadioGroup() {
  return (
    <Radio.Group defaultValue="pro" className="flex gap-2">
      {plans.map(({ value, label, description }) => (
        <Radio.Root key={value} value={value} className="flex-1 rounded-lg border p-3 cursor-pointer data-[checked]:border-blue-600 data-[checked]:bg-blue-50 border-gray-200">
          <div className="flex items-center justify-between mb-1">
            <span className="text-sm font-medium text-gray-800">{label}</span>
            <Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center data-[checked]:border-blue-600">
              <span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" />
            </Radio.Indicator>
          </div>
          <p className="text-xs text-gray-400">{description}</p>
        </Radio.Root>
      ))}
    </Radio.Group>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Radio.Group defaultValue="pro" className="flex gap-2 w-full max-w-sm">
          <Radio.Root value="free" className="flex-1 rounded-lg border p-3 cursor-pointer data-[checked]:border-blue-600 data-[checked]:bg-blue-50 border-gray-200">
            <div className="flex items-center justify-between mb-1"><span className="text-sm font-medium text-gray-800">Free</span><Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center data-[checked]:border-blue-600"><span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" /></Radio.Indicator></div>
            <p className="text-xs text-gray-400">Up to 3 projects</p>
          </Radio.Root>
          <Radio.Root value="pro" className="flex-1 rounded-lg border p-3 cursor-pointer data-[checked]:border-blue-600 data-[checked]:bg-blue-50 border-gray-200">
            <div className="flex items-center justify-between mb-1"><span className="text-sm font-medium text-gray-800">Pro</span><Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center data-[checked]:border-blue-600"><span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" /></Radio.Indicator></div>
            <p className="text-xs text-gray-400">$9/mo, unlimited</p>
          </Radio.Root>
          <Radio.Root value="team" className="flex-1 rounded-lg border p-3 cursor-pointer data-[checked]:border-blue-600 data-[checked]:bg-blue-50 border-gray-200">
            <div className="flex items-center justify-between mb-1"><span className="text-sm font-medium text-gray-800">Team</span><Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center data-[checked]:border-blue-600"><span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" /></Radio.Indicator></div>
            <p className="text-xs text-gray-400">$29/mo, 5 seats</p>
          </Radio.Root>
        </Radio.Group>
    </Tabs.Panel>
</Tabs.Root>

### Example 3 — Radio group with disabled option

<Tabs.Root className="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm" defaultValue="preview">
    <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
        <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
        <Tabs.Indicator className="absolute bottom-0 left-0 h-0.5 w-[var(--active-tab-width)] translate-x-[var(--active-tab-left)] rounded-full bg-blue-600 transition-all duration-200 ease-in-out" />
    </Tabs.List>
    <Tabs.Panel value="code">
```tsx
export default function DisabledRadio() {
  return (
    <Radio.Group defaultValue="email" className="flex flex-col gap-2">
      <Radio.Root value="email" className="flex items-center gap-2 cursor-pointer">
        <Radio.Indicator className="size-4 rounded-full border border-gray-300 data-[checked]:border-blue-600 flex items-center justify-center">
          <span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" />
        </Radio.Indicator>
        <span className="text-sm text-gray-700">Email</span>
      </Radio.Root>
      <Radio.Root value="sms" className="flex items-center gap-2 cursor-pointer">
        <Radio.Indicator className="size-4 rounded-full border border-gray-300 data-[checked]:border-blue-600 flex items-center justify-center">
          <span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" />
        </Radio.Indicator>
        <span className="text-sm text-gray-700">SMS</span>
      </Radio.Root>
      <Radio.Root value="push" disabled className="flex items-center gap-2 cursor-not-allowed opacity-40">
        <Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center">
          <span className="size-2 rounded-full bg-gray-400 opacity-0" />
        </Radio.Indicator>
        <span className="text-sm text-gray-400">Push (unavailable)</span>
      </Radio.Root>
    </Radio.Group>
  );
}
```
    </Tabs.Panel>
    <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
        <Radio.Group defaultValue="email" className="flex flex-col gap-2">
          <Radio.Root value="email" className="flex items-center gap-2 cursor-pointer">
            <Radio.Indicator className="size-4 rounded-full border border-gray-300 data-[checked]:border-blue-600 flex items-center justify-center"><span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" /></Radio.Indicator>
            <span className="text-sm text-gray-700">Email</span>
          </Radio.Root>
          <Radio.Root value="sms" className="flex items-center gap-2 cursor-pointer">
            <Radio.Indicator className="size-4 rounded-full border border-gray-300 data-[checked]:border-blue-600 flex items-center justify-center"><span className="size-2 rounded-full bg-blue-600 opacity-0 data-[checked]:opacity-100" /></Radio.Indicator>
            <span className="text-sm text-gray-700">SMS</span>
          </Radio.Root>
          <Radio.Root value="push" disabled className="flex items-center gap-2 cursor-not-allowed opacity-40">
            <Radio.Indicator className="size-4 rounded-full border border-gray-300 flex items-center justify-center"><span className="size-2 rounded-full bg-gray-400 opacity-0" /></Radio.Indicator>
            <span className="text-sm text-gray-400">Push (unavailable)</span>
          </Radio.Root>
        </Radio.Group>
    </Tabs.Panel>
</Tabs.Root>

## RadioIndicator

Indicates whether the radio button is selected.
Renders a `&lt;span&gt;` element.

Documentation: [Base UI Radio](https://base-ui.com/react/components/radio)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | `false` | Whether to keep the HTML element in the DOM when the radio button is inactive. |


---

## RadioRoot

Represents the radio button itself.
Renders a `&lt;span&gt;` element and a hidden `&lt;input&gt;` beside.

Documentation: [Base UI Radio](https://base-ui.com/react/components/radio)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **value*** | `Value` | — | The unique identifying value of the radio in a group. |
| **disabled** | `boolean` | — | Whether the component should ignore user interaction. |
| **required** | `boolean` | — | Whether the user must choose a value before submitting a form. |
| **readOnly** | `boolean` | — | Whether the user should be unable to select the radio button. |
| **inputRef** | `Ref<HTMLInputElement>` | — | A ref to access the hidden input element. |
| **nativeButton** | `boolean` | `false` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `true` if the rendered element is a native button. |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## RadioGroup

Provides a shared state to a series of radio buttons.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Radio Group](https://base-ui.com/react/components/radio)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **disabled** | `boolean` | `false` | Whether the component should ignore user interaction. |
| **readOnly** | `boolean` | `false` | Whether the user should be unable to select a different radio button in the group. |
| **required** | `boolean` | `false` | Whether the user must choose a value before submitting a form. |
| **name** | `string` | — | Identifies the field when a form is submitted. |
| **value** | `Value` | — | The controlled value of the radio item that should be currently selected.<br/><br/>To render an uncontrolled radio group, use the `defaultValue` prop instead. |
| **defaultValue** | `Value` | — | The uncontrolled value of the radio button that should be initially selected.<br/><br/>To render a controlled radio group, use the `value` prop instead. |
| **onValueChange** | `(...) => void` | — | Callback fired when the value changes. |
| **inputRef** | `Ref<HTMLInputElement>` | — | A ref to access the hidden input element. |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

