---
title: Popover
description: An accessible popup anchored to a button.
---

# Popover

An accessible popup anchored to a trigger element. Supports title, description, close button, and rich content.

<Tabs.Root className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm" defaultValue="preview">
  <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
  </Tabs.List>
  <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
    <Popover.PopoverBasic />
  </Tabs.Panel>
  <Tabs.Panel className="p-0 outline-none" value="code">
    ```tsx
    import { Popover } from "gnome-ui/popover";
    import { Button } from "@/components/ui/button";

    export function PopoverBasic() {
      return (
        <Popover.Root>
          <Popover.Trigger
            render={<Button variant="outline"><Icons.Bell /> Notifications</Button>}
          />
          <Popover.Portal>
            <Popover.Positioner sideOffset={8}>
              <Popover.Popup>
                <Popover.Title>Notifications</Popover.Title>
                <Popover.Description>
                  You're all caught up. No new notifications right now.
                </Popover.Description>
              </Popover.Popup>
            </Popover.Positioner>
          </Popover.Portal>
        </Popover.Root>
      );
    }
    ```
  </Tabs.Panel>
</Tabs.Root>

# Anatomy

```tsx
import { Popover } from "gnome-ui/popover";

<Popover.Root>
  <Popover.Trigger />
  <Popover.Portal>
    <Popover.Positioner>
      <Popover.Popup>
        <Popover.Title />
        <Popover.Description />
        <Popover.Close />
      </Popover.Popup>
    </Popover.Positioner>
  </Popover.Portal>
</Popover.Root>
```

# Examples

## With Close Button

Use `Popover.Close` to give the user explicit close control. Useful in informational or onboarding popovers.

<Tabs.Root className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm" defaultValue="preview">
  <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
  </Tabs.List>
  <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
    <Popover.PopoverWithClose />
  </Tabs.Panel>
  <Tabs.Panel className="p-0 outline-none" value="code">
    ```tsx
    export function PopoverWithClose() {
      return (
        <Popover.Root>
          <Popover.Trigger
            render={<Button variant="outline"><Icons.Info /> What's new</Button>}
          />
          <Popover.Portal>
            <Popover.Positioner sideOffset={8}>
              <Popover.Popup>
                <div className="p-4">
                  <div className="flex items-start justify-between gap-2 mb-3">
                    <Popover.Title>Release 2.4.0</Popover.Title>
                    <Popover.Close aria-label="Close">
                      <Icons.X />
                    </Popover.Close>
                  </div>
                  <Popover.Description>
                    We've shipped new keyboard shortcuts, improved search performance,
                    and fixed several reported bugs in the dashboard.
                  </Popover.Description>
                  <a href="#">
                    Read full changelog <Icons.ExternalLink />
                  </a>
                </div>
              </Popover.Popup>
            </Popover.Positioner>
          </Popover.Portal>
        </Popover.Root>
      );
    }
    ```
  </Tabs.Panel>
</Tabs.Root>

---

## Rich Content

The popup can contain any content. This example shows a user profile with metadata and actions.

<Tabs.Root className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm" defaultValue="preview">
  <Tabs.List className="relative z-0 flex border-b border-gray-200 bg-gray-50 px-2">
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="preview">Preview</Tabs.Tab>
    <Tabs.Tab className="relative flex h-10 min-w-[96px] cursor-pointer items-center justify-center px-4 text-center text-sm font-medium whitespace-nowrap text-gray-500 outline-none select-none transition-colors duration-150 hover:text-gray-800 focus-visible:z-10 focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:rounded data-[active]:text-gray-900 data-[active]:font-semibold" value="code">Code</Tabs.Tab>
  </Tabs.List>
  <Tabs.Panel className="flex min-h-[160px] items-center justify-center p-6 outline-none" value="preview">
    <Popover.PopoverRichContent />
  </Tabs.Panel>
  <Tabs.Panel className="p-0 outline-none" value="code">
    ```tsx
    const user = {
      name: "Sara Müller",
      role: "Product Designer",
      email: "sara@example.com",
      location: "Berlin, Germany",
      joined: "March 2022",
    };

    export function PopoverRichContent() {
      return (
        <Popover.Root>
          <Popover.Trigger
            render={
              <button aria-label="User profile">
                <span>SM</span>
              </button>
            }
          />
          <Popover.Portal>
            <Popover.Positioner sideOffset={10} side="bottom" align="end">
              <Popover.Popup>
                {/* Header */}
                <div className="flex items-center gap-3 border-b p-4">
                  <span>SM</span>
                  <div>
                    <Popover.Title>{user.name}</Popover.Title>
                    <Popover.Description>{user.role}</Popover.Description>
                  </div>
                </div>

                {/* Meta */}
                <div className="space-y-2 p-4 border-b">
                  <div><Icons.Mail /> {user.email}</div>
                  <div><Icons.MapPin /> {user.location}</div>
                  <div><Icons.Calendar /> Joined {user.joined}</div>
                </div>

                {/* Actions */}
                <div className="p-2">
                  <button><Icons.User /> View profile</button>
                  <button><Icons.Settings /> Account settings</button>
                  <button><Icons.LogOut /> Log out</button>
                </div>
              </Popover.Popup>
            </Popover.Positioner>
          </Popover.Portal>
        </Popover.Root>
      );
    }
    ```
  </Tabs.Panel>
</Tabs.Root>

## PopoverArrow

Displays an element positioned against the popover anchor.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## PopoverBackdrop

An overlay displayed beneath the popover.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## PopoverClose

A button that closes the popover.
Renders a `&lt;button&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **nativeButton** | `boolean` | `true` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `false` if the rendered element is not a button (e.g. `&lt;div&gt;`). |


---

## PopoverDescription

A paragraph with additional information about the popover.
Renders a `&lt;p&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## PopoverPopup

A container for the popover contents.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **initialFocus** | `boolean \| RefObject<HTMLElement> \| (...) => boolean \| void \| HTMLElement)` | — | Determines the element to focus when the popover is opened.<br/><br/>- `false`: Do not move focus.<br/>- `true`: Move focus based on the default behavior (first tabbable element or popup).<br/>- `RefObject`: Move focus to the ref element.<br/>- `function`: Called with the interaction type (`mouse`, `touch`, `pen`, or `keyboard`).<br/>  Return an element to focus, `true` to use the default behavior, or `false`/`undefined` to do nothing. |
| **finalFocus** | `boolean \| RefObject<HTMLElement> \| (...) => boolean \| void \| HTMLElement)` | — | Determines the element to focus when the popover is closed.<br/><br/>- `false`: Do not move focus.<br/>- `true`: Move focus based on the default behavior (trigger or previously focused element).<br/>- `RefObject`: Move focus to the ref element.<br/>- `function`: Called with the interaction type (`mouse`, `touch`, `pen`, or `keyboard`).<br/>  Return an element to focus, `true` to use the default behavior, or `false`/`undefined` to do nothing. |


---

## PopoverPortal

A portal element that moves the popup to a different part of the DOM.
By default, the portal element is appended to `&lt;body&gt;`.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **keepMounted** | `boolean` | `false` | Whether to keep the portal mounted in the DOM while the popup is hidden. |
| **container** | `HTMLElement \| ShadowRoot \| RefObject<HTMLElement \| ShadowRoot>` | — | A parent element to render the portal element into. |


---

## PopoverPositioner

Positions the popover against the trigger.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **anchor** | `Element \| RefObject<Element> \| VirtualElement \| (...) => Element \| VirtualElement)` | — | An element to position the popup against.<br/>By default, the popup will be positioned against the trigger. |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **sticky** | `boolean` | `false` | Whether to maintain the popup in the viewport after<br/>the anchor element was scrolled out of view. |
| **side** | `Side` | `'bottom'` | Which side of the anchor element to align the popup against.<br/>May automatically change to avoid collisions. |
| **align** | `Align` | `'center'` | How to align the popup relative to the specified side. |
| **positionMethod** | `"fixed" \| "absolute"` | `'absolute'` | Determines which CSS `position` property to use. |
| **sideOffset** | `number \| OffsetFunction` | `0` | Distance between the anchor and the popup in pixels.<br/>Also accepts a function that returns the distance to read the dimensions of the anchor<br/>and positioner elements, along with its side and alignment.<br/><br/>The function takes a `data` object parameter with the following properties:<br/>- `data.anchor`: the dimensions of the anchor element with properties `width` and `height`.<br/>- `data.positioner`: the dimensions of the positioner element with properties `width` and `height`.<br/>- `data.side`: which side of the anchor element the positioner is aligned against.<br/>- `data.align`: how the positioner is aligned relative to the specified side.<br/>@example  |
| **alignOffset** | `number \| OffsetFunction` | `0` | Additional offset along the alignment axis in pixels.<br/>Also accepts a function that returns the offset to read the dimensions of the anchor<br/>and positioner elements, along with its side and alignment.<br/><br/>The function takes a `data` object parameter with the following properties:<br/>- `data.anchor`: the dimensions of the anchor element with properties `width` and `height`.<br/>- `data.positioner`: the dimensions of the positioner element with properties `width` and `height`.<br/>- `data.side`: which side of the anchor element the positioner is aligned against.<br/>- `data.align`: how the positioner is aligned relative to the specified side.<br/>@example  |
| **collisionBoundary** | `Boundary` | `'clipping-ancestors'` | An element or a rectangle that delimits the area that the popup is confined to. |
| **collisionPadding** | `Padding` | `5` | Additional space to maintain from the edge of the collision boundary. |
| **arrowPadding** | `number` | `5` | Minimum distance to maintain between the arrow and the edges of the popup.<br/><br/>Use it to prevent the arrow element from hanging out of the rounded corners of a popup. |
| **disableAnchorTracking** | `boolean` | `false` | Whether to disable the popup from tracking any layout shift of its positioning anchor. |
| **collisionAvoidance** | `CollisionAvoidance` | — | Determines how to handle collisions when positioning the popup.<br/>@example  |


---

## PopoverRoot

Groups all parts of the popover.
Doesn’t render its own HTML element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **defaultOpen** | `boolean` | `false` | Whether the popover is initially open.<br/><br/>To render a controlled popover, use the `open` prop instead. |
| **open** | `boolean` | — | Whether the popover is currently open. |
| **onOpenChange** | `(...) => void` | — | Event handler called when the popover is opened or closed. |
| **onOpenChangeComplete** | `(...) => void` | — | Event handler called after any animations complete when the popover is opened or closed. |
| **actionsRef** | `RefObject<PopoverRootActions>` | — | A ref to imperative actions.<br/>- `unmount`: When specified, the popover will not be unmounted when closed.<br/>Instead, the `unmount` function must be called to unmount the popover manually.<br/>Useful when the popover's animation is controlled by an external library.<br/>- `close`: Closes the dialog imperatively when called. |
| **modal** | `boolean \| "trap-focus"` | `false` | Determines if the popover enters a modal state when open.<br/>- `true`: user interaction is limited to the popover: document page scroll is locked, and pointer interactions on outside elements are disabled.<br/>- `false`: user interaction with the rest of the document is allowed.<br/>- `'trap-focus'`: focus is trapped inside the popover, but document page scroll is not locked and pointer interactions outside of it remain enabled. |
| **triggerId** | `string` | — | ID of the trigger that the popover is associated with.<br/>This is useful in conjunction with the `open` prop to create a controlled popover.<br/>There's no need to specify this prop when the popover is uncontrolled (i.e. when the `open` prop is not set). |
| **defaultTriggerId** | `string` | — | ID of the trigger that the popover is associated with.<br/>This is useful in conjunction with the `defaultOpen` prop to create an initially open popover. |
| **handle** | `PopoverHandle<Payload>` | — | A handle to associate the popover with a trigger.<br/>If specified, allows external triggers to control the popover's open state. |
| **children** | `ReactNode \| PayloadChildRenderFunction<Payload>` | — | The content of the popover.<br/>This can be a regular React node or a render function that receives the `payload` of the active trigger. |


---

## PopoverTitle

A heading that labels the popover.
Renders an `&lt;h2&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

## PopoverTrigger

A button that opens the popover.
Renders a `&lt;button&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **nativeButton** | `boolean` | `true true` | Whether the component renders a native `&lt;button&gt;` element when replacing it<br/>via the `render` prop.<br/>Set to `false` if the rendered element is not a button (e.g. `&lt;div&gt;`). |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |
| **handle** | `PopoverHandle<Payload>` | — | A handle to associate the trigger with a popover. |
| **payload** | `Payload` | — | A payload to pass to the popover when it is opened. |
| **openOnHover** | `boolean` | `false` | Whether the popover should also open when the trigger is hovered. |
| **delay** | `number` | `300` | How long to wait before the popover may be opened on hover. Specified in milliseconds.<br/><br/>Requires the `openOnHover` prop. |
| **closeDelay** | `number` | `0` | How long to wait before closing the popover that was opened on hover.<br/>Specified in milliseconds.<br/><br/>Requires the `openOnHover` prop. |


---

## PopoverViewport

A viewport for displaying content transitions.
This component is only required if one popup can be opened by multiple triggers, its content change based on the trigger
and switching between them is animated.
Renders a `&lt;div&gt;` element.

Documentation: [Base UI Popover](https://base-ui.com/react/components/popover)

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| **className** | `string \| (...) => string)` | — | CSS class applied to the element, or a function that<br/>returns a class based on the component’s state. |
| **children** | `ReactNode` | — | The content to render inside the transition container. |
| **render** | `ReactElement> \| ComponentRenderFn` | — | Allows you to replace the component’s HTML element<br/>with a different tag, or compose it with another component.<br/><br/>Accepts a `ReactElement` or a function that returns the element to render. |


---

